---
cmd-mesh: major
---

Added a shared toolkit for file, path, configuration, project, and workspace operations through direct imports and handler context. Hand-built `Ctx` values must spread the exported `toolkit` object.
